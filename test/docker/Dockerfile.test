FROM golang:1.25-alpine

RUN apk add --no-cache gcc musl-dev fuse-dev fuse

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# Default: run all integration tests with FUSE
ENTRYPOINT ["go", "test", "-count=1"]
CMD ["-v", "./test/integration/..."]
